# Workspace permission management

## Introduction

An **Attribute-Based Access Control (ABAC)** system that manages workspaces, user attributes, and permission policies. Users are dynamically assigned to workspaces based on attribute-matching membership rules, and permissions are evaluated at runtime via Open Policy Agent (OPA).